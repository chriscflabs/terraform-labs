terraform {
  required_providers {
    cloudflare = {
      source  = "cloudflare/cloudflare"
      version = "~> 4.0"
    }
  }
}

provider "cloudflare" {
  #Credentials Imported from $CLOUDFLARE_API_TOKEN environment variable
}

